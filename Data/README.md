# Data

Runtime data directory for game data tables, configurations, and serialized state.

Populated by the active project adapter during project load.
See `Source/Workspace/include/NF/Workspace/AssetCatalog.h` for the asset registry model.
