# This file is intentionally empty — the Cargo workspace root lives at the
# repository root (../Cargo.toml).  Run `cargo build` from the repository root.
#
# Keeping this file here only so that tools that expect a Cargo.toml inside
# AtlasRust/ don't error out.  It is NOT a workspace member.
